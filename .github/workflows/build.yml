name: Build Emerald Legacy ROM

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout repo
      uses: actions/checkout@v3
      with:
        submodules: recursive

    - name: 🔧 Install dependencies
      run: |
        sudo apt update
        sudo apt install -y build-essential libpng-dev python3

    - name: 🔨 Build internal tools
      run: |
        chmod +x build_tools.sh
        ./build_tools.sh

    - name: 🛠 Build ROM
      run: |
        make clean
        make -j$(nproc)

    - name: 📦 Upload ROM Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Emerald-Legacy-Mew
        path: '*.gba'
