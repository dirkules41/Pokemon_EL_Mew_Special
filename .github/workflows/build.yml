name: Build Emerald Legacy ROM

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout repo
      uses: actions/checkout@v3
      with:
        submodules: recursive

    - name: 🔧 Install dependencies
      run: |
        sudo apt update
        sudo apt install -y build-essential libpng-dev python3 wget tar xz-utils

    - name: 🔧 Install devkitARM (official APT repo)
      run: |
        sudo apt-get update
        sudo apt-get install -y apt-transport-https curl

        # Add devkitPro APT repo
        echo "deb https://apt.devkitpro.org/ stable main" | sudo tee /etc/apt/sources.list.d/devkitpro.list
        curl -L https://apt.devkitpro.org/devkitpro-pub.gpg | sudo tee /usr/share/keyrings/devkitpro-pub.gpg

        sudo apt-get update
        sudo apt-get install -y devkitpro-pacman

        sudo dkp-pacman -Syu --noconfirm
        sudo dkp-pacman -S --noconfirm devkitARM

        echo "DEVKITPRO=/opt/devkitpro" >> $GITHUB_ENV
        echo "DEVKITARM=/opt/devkitpro/devkitARM" >> $GITHUB_ENV
        echo "/opt/devkitpro/devkitARM/bin" >> $GITHUB_PATH


    - name: 🔨 Build internal tools
      run: |
        chmod +x build_tools.sh
        ./build_tools.sh

    - name: 🛠 Build ROM
      run: |
        make clean
        make -j$(nproc)

    - name: 📦 Upload ROM Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Emerald-Legacy-Mew
        path: '*.gba'
